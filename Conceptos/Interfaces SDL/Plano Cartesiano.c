#include <SDL2/SDL.h>

int main(){

    // Declaramos las variables que estaremos usando para el modo grafico

    // Declaramos la ventana
    SDL_Window *ventana = NULL;

    // Declaramos el renderizador
    SDL_Renderer *renderer = NULL;

    
}